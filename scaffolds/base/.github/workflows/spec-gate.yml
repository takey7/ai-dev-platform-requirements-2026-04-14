name: spec-gate

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  spec-gate:
    uses: {{SOURCE_REPO}}/.github/workflows/spec-gate.yml@{{SOURCE_REF}}
    with:
      manifest_path: .platform/platform.yaml
    secrets: inherit
