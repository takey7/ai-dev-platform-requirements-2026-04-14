name: ai-gate

on:
  pull_request:
  merge_group:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  ai-gate:
    uses: {{SOURCE_REPO}}/.github/workflows/ai-gate.yml@{{SOURCE_REF}}
    with:
      manifest_path: .platform/platform.yaml
    secrets: inherit
