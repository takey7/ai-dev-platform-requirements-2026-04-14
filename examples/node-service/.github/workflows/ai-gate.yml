name: ai-gate

on:
  pull_request:
  merge_group:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  ai-gate:
    uses: takey7/ai-dev-platform-requirements-2026-04-14/.github/workflows/ai-gate.yml@v0.1.5
    with:
      manifest_path: .platform/platform.yaml
    secrets: inherit
