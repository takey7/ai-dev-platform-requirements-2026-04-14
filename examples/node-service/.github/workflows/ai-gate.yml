name: ai-gate

on:
  pull_request:
  merge_group:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  ai-gate:
    uses: slm100/ai-dev-platform-requirements-2026-04-14/.github/workflows/ai-gate.yml@main
    with:
      manifest_path: .platform/platform.yaml
    secrets: inherit
