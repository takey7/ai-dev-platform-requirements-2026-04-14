name: release-ready

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  release-ready:
    uses: slm100/ai-dev-platform-requirements-2026-04-14/.github/workflows/release-ready.yml@main
    with:
      manifest_path: .platform/platform.yaml
    secrets: inherit
