name: release-ready

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  release-ready:
    uses: takey7/ai-dev-platform-requirements-2026-04-14/.github/workflows/release-ready.yml@v0.1.5
    with:
      manifest_path: .platform/platform.yaml
    secrets: inherit
