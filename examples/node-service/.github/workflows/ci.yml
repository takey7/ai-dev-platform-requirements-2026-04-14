name: ci

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  ci:
    uses: slm100/ai-dev-platform-requirements-2026-04-14/.github/workflows/ci.yml@main
    with:
      manifest_path: .platform/platform.yaml
    secrets: inherit
