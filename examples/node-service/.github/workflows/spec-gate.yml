name: spec-gate

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  spec-gate:
    uses: takey7/ai-dev-platform-requirements-2026-04-14/.github/workflows/spec-gate.yml@v0.1.5
    with:
      manifest_path: .platform/platform.yaml
    secrets: inherit
